shader contour(float r = 0., float increment = 0.5,float thickness=0.1, output color col = 1.){
    float val = r/increment;
    float i_val = round(r/increment);
    float c = 2* abs(val-i_val);
    c = pow(c,0.5);
    if (c<thickness)
        col = 0;
    else 
        col = 1;
    
}